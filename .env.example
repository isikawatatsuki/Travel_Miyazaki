# Optional: use another PMTiles v3 basemap archive instead of the bundled regional extract.
# VITE_PMTILES_URL=https://example.com/path/to/basemap.pmtiles

# Optional: use a compatible geocoding endpoint for map search.
# VITE_GEOCODER_URL=https://nominatim.openstreetmap.org/search

# Optional: use another Valhalla-compatible routing endpoint.
# VITE_ROUTER_URL=https://valhalla1.openstreetmap.de/route
